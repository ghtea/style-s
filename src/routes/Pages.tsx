import React from "react";

type PropsPages = {
  
};

function Pages({ }: PropsPages) {
  
  return (
    <div>
      pages
    </div>
  );
}

Pages.defaultProps = {
  
};

export default Pages;